import QueueService from "../QueueService";
import QueueEntry from "../../models/QueueEntry";

jest.mock('../../models/QueueEntry.js');

describe('QueueService', () => {
    beforeEach(() => {
        jest.clearAllMocks();
    });

    describe('addToQueue', () => {
        it('deve adicionar uma pessoa a fila', () => {
            
        })
    })
})